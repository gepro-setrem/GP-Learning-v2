<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--  Estilos Personalizados   -->

    <style name="InputLogin" parent="Widget.Design.TextInputLayout">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="imageEtapaProjeto">
        <item name="android:layout_width">85dp</item>
        <item name="android:layout_height">85dp</item>
        <item name="android:layout_marginTop">80dp</item>
        <!--<item name="android:elevation" tools:targetApi="lollipop">5dp</item>-->
    </style>

    <style name="txtEtapaProjeto">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ratingBarEtapaProjeto" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="textviewProcessosEtapaProjeto">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>

        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">100dp</item>
        <!--<item name="app:layout_constraintLeft_toLeftOf">parent</item>-->
        <!--<item name="app:layout_constraintRight_toRightOf">parent</item>-->

    </style>


</resources>
